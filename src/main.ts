// TempleOS Browser — Main entry point
// This will be expanded in the frontend-skeleton feature.

const app = document.getElementById('app');
if (app) {
  app.innerHTML = '<h1>TempleOS Browser</h1><p>Emulator assets loaded. Frontend coming soon.</p>';
}
